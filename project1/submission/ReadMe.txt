1.
Compile using gcc:

gcc -fopenmp -o tspp tsp-parallel.c 
  

gcc -o tsps tsp-serial.c

2.
Execute:

./tspp filename.txt

./tsps filename.txt

3.
The program assumes the city names are numbers.

4.
The script assumes 3 test files will be tested, and their names are 5.txt, 10.txt and 15.txt.

5. 
The program reads the information from a .txt file and uses the standard output.